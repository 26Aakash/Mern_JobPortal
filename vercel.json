{
  "buildCommand": "cd job-portal-client && npm install && npm run build",
  "outputDirectory": "job-portal-client/dist",
  "env": {
    "VITE_API_BASE_URL": "@vite_api_base_url"
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
